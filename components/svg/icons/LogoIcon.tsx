import React from 'react';

/**
 * Logo image removed per user direction:
 * Brand is now represented purely by minimalist text typography across the app.
 */
const LogoIcon = (_props: any) => null;

export default LogoIcon;
